{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": { "dockerfile": "Dockerfile" }
    }
  ],
  "routes": [
    {
      "src": "/api/generate-pdf",
      "dest": "/api/generate-pdf.ts"
    }
  ]
}
