{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": { "dockerfile": "Dockerfile" }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      }
    ]
  }